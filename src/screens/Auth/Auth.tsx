import React from 'react'
import styles from './Auth.module.scss'

const Auth: React.FC = () => {
	return (
		<div className={styles.root}>
			<h1>Authorization</h1>
		</div>
	)
}

export default Auth
